import styled from 'styled-components';

const Layout = styled.section`
  padding: 1em;
  max-width: 70%;
  margin: auto;
  height: 100%;
  display: flex;
  flex-direction: column;
`;

const S = {
  Layout,
};

export default S;
